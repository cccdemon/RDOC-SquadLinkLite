# Rust
/target
**/target
Cargo.lock.orig

# Node / Tauri frontend
node_modules
dist
.vite

# Tauri build output
apps/companion/src-tauri/target

# Env / secrets
.env
.env.*
!.env.example

# Self-signed certs / keys (never commit)
*.pem
*.key
*.log

# OS
Thumbs.db
.DS_Store

# Local CI-downloaded test binaries (installers + MSIX)
test-build/

# Stream Deck plugin: built artifact (packed at release time)
streamdeck-plugin/subraum.streamDeckPlugin
