[package]
name = "companion-core"
version = "0.1.0"
edition.workspace = true

# Headless 2-peer (→ N-peer) mesh client: cpal+opus audio over webrtc-rs P2P,
# per-peer DataChannel chat, signaling to InitConnection. UI comes later.

[lib]
name = "companion_core"
path = "src/lib.rs"

[[bin]]
name = "companion-core"
path = "src/main.rs"

[dependencies]
protocol = { path = "../../crates/protocol" }
serde = { version = "1", features = ["derive"] }
webrtc = "0.17"
audiopus = "0.2"
nnnoiseless = { version = "0.5", default-features = false }
cpal = "0.18"
tokio = { version = "1", features = ["full"] }
tokio-tungstenite = { version = "0.29", features = ["rustls-tls-webpki-roots"] }
rustls = { version = "0.23", features = ["ring"] }
sha2 = "0.11"
hex = "0.4"
# Post-quantum hybrid handshake (app-layer, over the DTLS DataChannel).
ml-kem = "0.3"
x25519-dalek = "3"
hkdf = "0.13"
chacha20poly1305 = "0.11"
rand_core = { version = "0.6", features = ["getrandom"] }
futures = "0.3"
serde_json = "1"
base64 = "0.23"
bytes = "1"
anyhow = "1"
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }

[dev-dependencies]
# `test-util` gives the tests a paused clock, so the room-key grace period
# (ROOM_KEY_GRACE) elapses instantly instead of really sleeping.
tokio = { version = "1", features = ["full", "test-util"] }
